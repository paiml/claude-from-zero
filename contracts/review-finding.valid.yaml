# contracts/review-finding.valid.yaml
#
# Realistic sub-agent output that passes every invariant in
# review-finding.yaml. Used as a fixture by m4-review's unit tests
# and as a hand-runnable demo for the validate-finding binary.

reviewer: explore-agent-pass-1
target: m4-review/src/bin/diff_target.rs
findings:
  - severity: info
    line: 17
    message: "square() uses checked_mul — overflow returns None, which is caught at line 40."
    category: correctness
  - severity: warning
    line: 50
    message: "assert! restates the invariant; consider a #[cfg(debug_assertions)] guard to avoid paying the check in release."
    category: performance
  - severity: info
    line: 47
    message: "End-of-main comment documents the contract proof; reviewers should preserve this line."
    category: readability
verdict: comment
rationale: >-
  The diff_target.rs binary holds its stated contract: it parses one
  integer argument, squares it via checked_mul, and exits 2 on any
  precondition violation. The assert!/eprintln! pair at end of main
  is the runtime proof. No correctness issues; one minor performance
  suggestion.
